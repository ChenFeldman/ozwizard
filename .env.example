# OzWizard configuration (environment only — no hardcoded values).
PORT=3000
HOST=0.0.0.0
DATA_PATH=./data/ozwizard.json
# Log level: fatal | error | warn | info | debug | trace
LOG_LEVEL=info
# NODE_ENV=development enables pretty logs.
NODE_ENV=development
