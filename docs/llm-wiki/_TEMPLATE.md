# <Page title — the topic, not the file name>

> **When to load this:** <One sentence naming the concrete trigger — the kind of
> task or question for which an agent should read this page before acting. Be
> specific enough that "does this apply?" is answerable at a glance.>

## Summary

<3–5 lines. The load-bearing takeaways, stated plainly, so an agent that reads
only this block already avoids the most common mistake. If someone reads nothing
else on the page, this is what they must come away with.>

## <Detailed section — stable heading>

<The long-form knowledge. Use stable headings (don't rename them casually — other
docs and prompts may point at them). Prefer concrete rules, tables, and named
examples from this repo over abstract prose. State the gotcha _and_ why it's a
gotcha.>

## <Another section — stable heading>

<...>

## Gotchas newcomers miss

- <A short, scannable list of the traps. Each item: the trap, then the fix.>

## Related

- <Links to other wiki pages, `docs/**` files, or `src/**` paths that go deeper.>

---

<!--
Structure rules for every wiki page:
  1. Title = the topic.
  2. "When to load this" blockquote = the load trigger, one sentence.
  3. Summary = 3–5 lines, the must-know.
  4. Detailed sections with STABLE headings (things may link to them).
  5. "Last pruned" line at the very bottom (below).
Keep pages self-contained: this is load-on-demand reference, not always-on context.
Prune on a schedule — delete what's stale, don't let pages rot.
-->

**Last pruned:** <YYYY-MM-DD> — <who / what was checked>
