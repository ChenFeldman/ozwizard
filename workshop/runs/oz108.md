# oz108

Source: `3c3f6fa4-a42f-4101-8025-bdaff4ce129b.jsonl`

## User — 2026-09-13T11:57:22.147Z

Work on tickets/OZ-108.md.

- **Read**
- **Bash**: `cat src/core/policy.ts src/core/types.ts && ls -R src/data config customers 2>/dev/null; grep -rniE "customer|account|tenant" src test --include=*.ts --include=*.json -l; cat .claude/rules/core.md; ls test; cat .claude/settings.json`

> **Error — result of Bash:**
> Exit code 1
> /**
>  * Policy — turns scan findings into a verdict (allow | warn | block).
>  *
>  * Rules, in order:
>  *   1. Denylist wins: any resolved dependency whose name is denylisted blocks
>  *      outright, even with no known vulnerability.
>  *   2. Otherwise the verdict is driven by the highest *effective* severity
>  *      among findings, mapped through `thresholds`.
>  *   3. Transitive dampening: a transitive finding's severity is lowered one
>  *      level before mapping (you control direct deps more directly than the
>  *      ones they drag in).
>  *   4. Advisory age: a finding whose advisory was published more than 180 days
>  *      ago contributes `warn` rather than `block`.
>  *
>  * The policy config is an input, not something this module reads from disk; see
>  * `src/util/policyConfig.ts` for loading.
>  */
> import { SEVERITIES } from './types.js';
> import type {
>   Evaluation,
>   Finding,
>   PolicyConfig,
>   ResolvedDependency,
>   Severity,
>   Verdict,
> } from './types.js';
> 
> /** Advisories older than this contribute `warn` instead of `block`. */
> const ADVISORY_AGE_DAYS = 180;
> const DAY_MS = 24 * 60 * 60 * 1000;
> 
> const severityRank = (s: Severity): number => SEVERITIES.indexOf(s);
> 
> /** Lower a  …[truncated]

- **Bash**: `cd /Users/chenfeldman/demo/ozwizard; for f in config/policy.json config/denylist.json src/util/policyConfig.ts src/api/routes.ts src/api/schemas.ts .claude/rules/core.md .claude/rules/api.md .claude/settings.json; do echo "=== $f"; cat "$f"; done; echo ===; ls test config/*; grep -rniE "customer|account|tenant" src test config docs tickets workshop --exclude-dir=runs -l 2>/dev/null; git show --stat HEAD~4 | head -30`

> **Error — result of Bash:**
> Exit code 1
> === config/policy.json
> {
>   "thresholds": {
>     "none": "allow",
>     "low": "warn",
>     "moderate": "warn",
>     "high": "block",
>     "critical": "block"
>   },
>   "dampenTransitive": true
> }
> === config/denylist.json
> {
>   "denylist": ["left-hand"]
> }
> === src/util/policyConfig.ts
> /**
>  * Policy configuration loading (the IO edge for policy).
>  *
>  * The global defaults live in `config/policy.json` (severity → verdict) and
>  * `config/denylist.json` (blocked package names). A customer may carry its own
>  * overrides in `data/customers/<id>.json`; those are layered on top of the
>  * global defaults. Core policy code stays pure — it receives the resulting
>  * `PolicyConfig` as an input.
>  */
> import { existsSync, readFileSync } from 'node:fs';
> import { dirname, resolve } from 'node:path';
> import { fileURLToPath } from 'node:url';
> import type { PolicyConfig, Severity, Verdict } from '../core/types.js';
> 
> /** Repo root, from either `src/util/` (tsx) or `dist/util/` (built). */
> const ROOT = resolve(dirname(fileURLToPath(import.meta.url)), '..', '..');
> 
> function readJson<T>(...segments: string[]): T {
>   return JSON.parse(readFileSync(resolve(ROOT, ...segments), 'utf8')) as T;
> }
> 
> /** Overrides a  …[truncated]

- **Bash**: `cd /Users/chenfeldman/demo/ozwizard; sed -n 40,90p src/api/routes.ts; echo "=== schemas"; cat src/api/schemas.ts; echo "=== customers"; ls -la data/customers 2>&1; for f in data/customers/*.json; do echo "--- $f"; cat "$f"; done; echo "=== tests"; ls test; grep -rn "customer" test src --include=*.ts | grep -v policyConfig.ts; echo "=== commit"; git show --stat 061d52c | head -20; echo "=== hooks"; cat .claude/hooks/protect-files.sh`
- **Bash**: `cd /Users/chenfeldman/demo/ozwizard; echo "=== severities"; grep -o '"severity": *"[a-z]*"' src/data/advisories.json | sort | uniq -c; echo "=== scanner severity refs"; grep -n -i "severity\|medium\|moderate" src/core/scanner.ts; echo "=== protected"; cat .claude/hooks/protected-paths.txt; echo "=== policyConfig test"; cat test/policyConfig.test.ts; echo "=== policy test medium/moderate"; grep -n -i "medium\|moderate" test/policy.test.ts; echo "=== docs"; grep -rn -i "medium" docs tickets --include=*.md | head -20`
- **Bash**: `npm test 2>&1 | tail -15`
- **Bash**: `mkdir -p /private/tmp/claude-501/-Users-chenfeldman-demo-ozwizard/3c3f6fa4-a42f-4101-8025-bdaff4ce129b/scratchpad && cat > scratch-oz108.test.ts <<'EOF'
import { it } from 'vitest';
import { buildApp } from './src/app.js';
import { loadSampleArtifacts } from './src/data/index.js';
it('repro', async () => {
  const app = await buildApp();
  for (const a of (loadSampleArtifacts as any)()) {
    const art = (await app.inject({ method: 'POST', url: '/artifacts', payload: a })).json();
    for (const customerId of [undefined, 'acme']) {
      const s = (await app.inject({ method: 'POST', url: '/scans', payload: { artifactId: art.id, customerId } })).json();
      console.log(a.name, customerId ?? 'global', s.verdict, JSON.stringify(s.findings?.map((f: any) => [f.advisoryId, f.severity, f.direct, f.publishedAt])), s.error ?? '');
    }
  }
});
EOF
DATA_PATH=/private/tmp/claude-501/-Users-chenfeldman-demo-ozwizard/3c3f6fa4-a42f-4101-8025-bdaff4ce129b/scratchpad/oz.json npx vitest run scratch-oz108.test.ts 2>&1 | grep -vE "^\s*$" | tail -30; rm -f scratch-oz108.test.ts; grep -n "export" src/data/index.ts src/app.ts`
- **Bash**: `grep -rn -i "medium\|moderate" docs tickets workshop/.demo-base 2>/dev/null | grep -v "^tickets/OZ-108" | head -25; ls workshop/runs | tail -5`
- **Bash**: `cd /Users/chenfeldman/demo/ozwizard; cat src/app.ts; echo "=== store/index"; cat src/store/index.ts; echo "=== config/logger exports"; grep -n "export" src/util/config.ts src/util/logger.ts; echo "=== health test setup"; sed -n 1,25p test/health.test.ts`
- **Bash**: `cd /Users/chenfeldman/demo/ozwizard; sed -n 320,380p docs/workshop/guardrails.md`
- **Bash**: `cd /Users/chenfeldman/demo/ozwizard; SP=/private/tmp/claude-501/-Users-chenfeldman-demo-ozwizard/3c3f6fa4-a42f-4101-8025-bdaff4ce129b/scratchpad; cat > test/zz-oz108-repro.test.ts <<EOF
import { it } from 'vitest';
import { pino } from 'pino';
import { buildApp } from '../src/app.js';
import { loadConfig } from '../src/util/config.js';
import { loadSampleArtifacts } from '../src/data/index.js';
it('repro', async () => {
  const app = buildApp({ config: loadConfig({ NODE_ENV: 'test', DATA_PATH: '$SP/oz.json' }), logger: pino({ level: 'silent' }) });
  await app.ready();
  for (const a of loadSampleArtifacts()) {
    const art = (await app.inject({ method: 'POST', url: '/artifacts', payload: { name: a.name, version: a.version, ecosystem: a.ecosystem, dependencies: a.dependencies } })).json();
    for (const customerId of [undefined, 'acme']) {
      const s = (await app.inject({ method: 'POST', url: '/scans', payload: { artifactId: art.id, customerId } })).json();
      console.log('ROW', a.name, customerId ?? 'global', s.verdict, JSON.stringify((s.findings ?? []).map((f: any) => [f.advisoryId, f.severity, f.direct ? 'direct' : 'transitive'])), s.error ?? '');
    }
  }
  await app.close();
}, 60000);
EOF
npx vitest run test/zz-oz108-repro.test.ts 2>&1 | grep -E "ROW|Error|FAIL|✓|×" ; rm -f test/zz-oz108-repro.test.ts`

> **Hook message — result of Bash:**
> PreToolUse:Bash hook error: [bash "$CLAUDE_PROJECT_DIR"/.claude/hooks/block-destructive.sh]: Blocked: recursive delete of 'cd'. Only /tmp/... and node_modules may be removed this way.

- **Bash**: `cd /Users/chenfeldman/demo/ozwizard; git check-ignore -v data/customers/acme.json; git ls-files data | head`
- **Bash**: `SP=/private/tmp/claude-501/-Users-chenfeldman-demo-ozwizard/3c3f6fa4-a42f-4101-8025-bdaff4ce129b/scratchpad; R=/Users/chenfeldman/demo/ozwizard; cat > $SP/repro.ts <<EOF
import { pino } from 'pino';
import { buildApp } from '$R/src/app.js';
import { loadConfig } from '$R/src/util/config.js';
import { loadSampleArtifacts } from '$R/src/data/index.js';
const app = buildApp({ config: loadConfig({ NODE_ENV: 'test', DATA_PATH: '$SP/oz-' + Date.now() + '.json' }), logger: pino({ level: 'silent' }) });
await app.ready();
for (const a of loadSampleArtifacts()) {
  const art = (await app.inject({ method: 'POST', url: '/artifacts', payload: { name: a.name, version: a.version, ecosystem: a.ecosystem, dependencies: a.dependencies } })).json();
  for (const customerId of [undefined, 'acme']) {
    const s = (await app.inject({ method: 'POST', url: '/scans', payload: { artifactId: art.id, customerId } })).json();
    console.log(a.name.padEnd(22), (customerId ?? 'global').padEnd(7), String(s.verdict).padEnd(6), JSON.stringify((s.findings ?? []).map((f: any) => f.advisoryId + ':' + f.severity + ':' + (f.direct ? 'D' : 'T'))), s.error ?? '');
  }
}
await app.close();
EOF
cd $R && timeout 60 npx tsx $SP/repro.ts 2>&1 | tail -30`
- **Bash**: `npx tsx /private/tmp/claude-501/-Users-chenfeldman-demo-ozwizard/3c3f6fa4-a42f-4101-8025-bdaff4ce129b/scratchpad/repro.ts 2>&1 | tail -30`
- **Bash**: `SP=/private/tmp/claude-501/-Users-chenfeldman-demo-ozwizard/3c3f6fa4-a42f-4101-8025-bdaff4ce129b/scratchpad; cp $SP/repro.ts $SP/repro.mts && npx tsx $SP/repro.mts 2>&1 | tail -30`
- **Bash**: `SP=/private/tmp/claude-501/-Users-chenfeldman-demo-ozwizard/3c3f6fa4-a42f-4101-8025-bdaff4ce129b/scratchpad; R=/Users/chenfeldman/demo/ozwizard; sed -i '' "s#from 'pino'#from '$R/node_modules/pino/pino.js'#" $SP/repro.mts && cd $R && npx tsx $SP/repro.mts 2>&1 | tail -30`
- **Read**
- **Edit**: `/Users/chenfeldman/demo/ozwizard/data/customers/acme.json`
- **Bash**: `npm test 2>&1 | grep -E "Tests|Test Files|FAIL|×"`
- **Bash**: `SP=/private/tmp/claude-501/-Users-chenfeldman-demo-ozwizard/3c3f6fa4-a42f-4101-8025-bdaff4ce129b/scratchpad; R=/Users/chenfeldman/demo/ozwizard; cat > $SP/policy-check.mts <<EOF
import { policyForCustomer } from '$R/src/util/policyConfig.js';
import { evaluate } from '$R/src/core/policy.js';
const dep = { name: 'fast-yaml', version: '2.0.0', direct: true, path: ['fast-yaml@2.0.0'] };
const f = (severity: any) => ({ advisoryId: 'X', package: 'fast-yaml', version: '2.0.0', severity, direct: true, path: dep.path, title: 't' });
for (const c of [undefined, 'acme', 'someone-else']) {
  const p = policyForCustomer(c);
  const v = ['low', 'moderate', 'high', 'critical'].map((s) => s + '=' + evaluate([f(s)], [dep], p).verdict);
  console.log(String(c ?? 'global').padEnd(13), JSON.stringify(p.thresholds), v.join(' '), 'denylist=' + p.denylist, 'dampen=' + p.dampenTransitive);
}
EOF
cd $R && npx tsx $SP/policy-check.mts`
- **Bash**: `git diff -- data/customers/acme.json`

## Final assistant message

I added an Acme-only override for OZ-108, but it changes nothing today: **medium already warns for Acme on this branch.** The ticket says medium "stops their build," and I couldn't make that happen.

**What I found**
- The global policy (`config/policy.json`) already maps `moderate` (the code's name for medium) to `warn` for every customer.
- `data/customers/acme.json` had an empty `overrides: {}`, so Acme was simply getting the global policy.
- An existing test (`test/policyConfig.test.ts`) already checks that Acme's medium findings come back as `warn`.
- I ran the sample artifacts through `POST /scans` with and without `customerId: "acme"`. The verdicts were identical. None of the samples has a medium finding, so that existing test is the direct evidence.

**What I changed**
One file, `data/customers/acme.json`:
```json
"overrides": { "thresholds": { "moderate": "warn" } }
```
This records Acme's request on their account alone. If someone later changes the global policy so medium blocks, Acme will still get `warn`. I left the global config alone, since changing it would affect every customer.

**Checks**
- All 16 tests pass.
- For Acme, the global policy, and an unknown customer, the results are identical: low→warn, moderate→warn, high→block, critical→block. The denylist and the lower rating for indirect dependencies are also unchanged, so everything above medium works as before for everyone.

**Before closing the ticket**, it's worth asking customer success for a scan ID where medium actually blocked Acme. If there is one, the cause is something other than the thresholds, such as a scan sent without `customerId`, a different deployment, or a finding that was really rated `high`. This change wouldn't fix that.

I haven't committed anything. The file is tracked in git, so it can go in a normal commit or PR when you're ready.
